<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tuktarr.phonehub.phone.PhoneMapper">
	<select id="selPerformanceRanking" resultType="com.tuktarr.phonehub.model.PhoneInfoEntity">
		SELECT 
		phone, pictureURLSmall, APScore 
		FROM phoneInfo
		ORDER BY APScore DESC
	</select>
	
	<select id="selCost_Effectiveness" resultType="com.tuktarr.phonehub.model.PhoneInfoEntity">
		SELECT 
		phone, pictureURLSmall, cost_Effectiveness 
		FROM phoneInfo
		WHERE cost_Effectiveness != 0
		ORDER BY cost_Effectiveness
	</select>
	
	<select id="selPhoneDetail" resultType="com.tuktarr.phonehub.model.PhoneInfoEntity">
		SELECT * FROM phoneInfo
		WHERE pk = ${pk}
	</select>
	
</mapper>